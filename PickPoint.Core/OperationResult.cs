namespace PickPoint.Core
{
    public enum OperationResult
    {
        ServerError,
        ClientError,
        NotFound,
        OutOfService,
        ValidationFailed,
        Locked,
        Success
    }
}
